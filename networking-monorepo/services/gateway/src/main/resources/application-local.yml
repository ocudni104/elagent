spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:nm_postgres}
    username: ${DB_USER:admin}
    password: ${DB_PASSWORD:admin}
  cloud:
    consul:
      discovery:
        enabled: false
        register: false
